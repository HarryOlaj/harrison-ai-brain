- Pam runtime was brought online as a separate OpenClaw profile on Jimmy host (`--profile pam`) with Discord account `Pam`; workspace aligned to shared repo `C:\ai_ops\harrison-ai-brain`.
- Persistent model drift issue observed in Pam: in-chat responses kept reporting Anthropic/billing errors even after Codex default was set; repeated evidence pointed to stale/in-flight session state and/or duplicate responder behavior with same bot token.
- Strong user preference reinforced after friction: agent-owned execution by default; avoid asking Harrison to run terminal/copy-paste steps unless a hard local interactive boundary is unavoidable.
- Secret-file workflow preference reinforced: use 1Password/LastPass-style storage and screenshots/context; agent should handle retrieval/usage end-to-end without chat-pasted secrets.
- Jimmy link outage RCA progressed: tunnel failures were caused by Mac LAN IP change (`192.168.50.169` -> `192.168.50.167`); tunnel/listener recovered after target update and hostname reachability (`Ais-MacBook-Pro.local`) validated.
- After transport recovery, node failures isolated to `device token mismatch`; next prescribed repair is gateway+node token reissue (`gateway install --force`, `node install --force --host 127.0.0.1 --port 18790`).
- Pam was brought online as her own runtime on Jimmy via `--profile pam` (separate from Jimmy main runtime), paired in Discord, and aligned to shared repo workspace (`C:\ai_ops\harrison-ai-brain`).
- Root-cause for repeated Pam billing errors: stale/in-flight Anthropic session state persisted in Pam session store; config changes alone were insufficient until runtime/session reset on the correct profile process.
- Google integration for Pam recovered after using downloaded OAuth client JSON (desktop client) and gog auth flow for `Harrison@up360.co`; API credential strings alone were insufficient.
- Strong operator preference reconfirmed during troubleshooting: agent should own execution end-to-end and only ask Harrison for unavoidable local interactive boundaries.
